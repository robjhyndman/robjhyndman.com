<ul class="recent-updates">
  <% for (const item of items) { %>
    <li>
      <a href="<%- item.path %>" class="recent-updates-title"><%- item.title %></a>
      <% if (item.date) { %> (<%- item.date %>)<% } %>
    </li>
    <% } %>
</ul>
